# Perceptron
Simple Perceptron -- Currently just a toy. Still need to create the structs for easier manipulation of layers, the backpropogation process is laid out currently in a more verbose fashion.
